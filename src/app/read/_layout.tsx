import { Stack } from "expo-router";

export default function LetterLayout() {
  return <Stack screenOptions={{ headerTintColor: "", title: "Read letter", headerStyle: {backgroundColor: "#443450"} }} />;
}